/**
 * useCart Hook
 * 
 * Re-export of the cart context hook for easier imports
 */

export { useCart } from '../contexts/CartContext';
export type { CartContextType } from '../types/cart';

// Additional cart-related hooks can be added here in the future
